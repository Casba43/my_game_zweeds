class: PlayerState
fields:
  inHand: List<CardModel>
  reserve: List<CardModel>     # player's 3-card reserve (face-up or stored)
  facedown: List<CardModel>    # player's 3 blind cards (face-down)
  stackCount: int              # remaining in table stack
  reserveCount: int
  blindCount: int
